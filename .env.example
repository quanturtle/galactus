# --- Postgres (shared metadata + scraper data) ---
POSTGRES_USER=the_scraper
POSTGRES_PASSWORD=the_scraper_secret
POSTGRES_DB=the_scraper
POSTGRES_PORT=5432

# Used by scraper code (CLI + Airflow tasks). Inside docker-compose, host is `db`;
# for running the CLI on the host, use `localhost`.
DATABASE_URL=postgresql://the_scraper:the_scraper_secret@localhost:5432/the_scraper

# --- Airflow ---
AIRFLOW_UID=50000
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__CORE__LOAD_EXAMPLES=False

# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
AIRFLOW__WEBSERVER__SECRET_KEY=

AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://the_scraper:the_scraper_secret@db:5432/airflow

# Initial admin user (seeded by airflow-init service on first boot).
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin
